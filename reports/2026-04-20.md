# 2026-04-20 - IMU Connection Struggles

- **19:01**: Ahmed reported zeros in `/imu/data` despite session establishment.
- **Troubleshooting**: Echo attempted to flash improved firmware with explicit I2C pins (`Wire.begin(21, 22)`) and timestamping.
- **Hardware Flaky**: ESP32 (CP2102) intermittently disappears from the USB bus (`/dev/ttyUSB0`). `lsusb` confirmed the device was missing after a flash attempt.
- **Action**: Echo successfully manually uploaded the firmware after killing a hung agent process, but the device disconnected immediately after "Hard resetting via RTS pin".
- **Current Block**: USB connectivity for ESP32 is unstable. Reset button has no effect when the device is missing from the bus.
